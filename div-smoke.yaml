# Short live check that the cap bites: module/python is the cell that produced three tasks from
# ONE repository this session, which is what prompted the knob. With max_per_repository 1 the
# same walk must spread across three different repositories instead.
output_dir: /data/evolvent/shijian-workdir/ffr-results/test
ledger_file: /data/evolvent/shijian-workdir/ffr-results/test/div-smoke.ledger.jsonl
checkpoint_file: /data/evolvent/shijian-workdir/ffr-results/test/div-smoke.checkpoint.json
freeze_runs: 5
max_concurrent: 3
e2b_max_active: 3
llm_max_concurrent: 3
sandboxed: true
jobs:
  - scale: module
    form: inplace
    source_language: python
    budget: 3
    max_attempts: 8
    max_per_repository: 1
