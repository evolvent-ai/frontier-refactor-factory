# Cell 1 of the remaining 14: repo/python.
#
# The cheapest one to close. Two subjects already exist on disk with 0 attested, and the reason is
# recorded: both were produced on the local backend, so nothing witnessed them in a sandbox. An
# expectation frozen on this host describes this host -- which is why `audit_matrix` shows the
# backend as "-" rather than "remote" and refuses to call them attested.
#
# Nothing needs fixing here. The material was already good enough once.
output_dir: /data/evolvent/shijian-workdir/ffr-results/test
ledger_file: /data/evolvent/shijian-workdir/ffr-results/test/cell-repo-python.ledger.jsonl
checkpoint_file: /data/evolvent/shijian-workdir/ffr-results/test/cell-repo-python.checkpoint.json
freeze_runs: 5
max_concurrent: 2
e2b_max_active: 2
llm_max_concurrent: 3
sandboxed: true
jobs:
  - scale: repo
    form: inplace
    source_language: python
    budget: 1
    max_attempts: 10
