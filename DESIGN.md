# frontier-refactor-factory — 设计方案

> 状态：**方案讨论中，尚未实现**。本文件是动手前要先谈拢的东西。
>
> 目标：一条链路，产出四个尺度的重构任务 —— **kernel / module / package / repo**。
> 能统一的统一，必须分叉的分叉，但每一处分叉都要说得出「为什么它不能统一」。

---

## 0. 术语（先把话说清楚）

旧链路里的黑话各不相同，这里定死一套。

| 术语 | 含义 |
|---|---|
| **Subject** | 被改造的东西：一个计算例程 / 一个函数 / 一个包 / 一整个仓库 |
| **Probe** | 一次探测：喂给 Subject 的一组输入 |
| **Expectation** | 正确行为的记录。**测出来的，不是人写的** |
| **Verifier** | 判分器：跑候选 → 比对 Expectation → 给分 |
| **Evidence** | 出厂前对 **Verifier 自身** 的检验 |
| **Headroom** | 这道题究竟有没有可赚的优化空间 |

**Verifier 和 Evidence 的区别是本设计最重要的一条，务必分清：**

- **Verifier 判「提交对不对」** —— 面向解题者
- **Evidence 判「Verifier 自己诚不诚实」** —— 面向我们

一个永远给零分的 Verifier，能通过所有「错误提交必须得零分」的检验。所以 Evidence 必须
**两头都咬**：参考实现必须拿满分，胡乱提交必须得零分。缺任何一头，另一头都不成立。

---

## 1. 四个尺度，两种接缝

要判断「改写后行为变没变」，必须在某条**接缝**上观察 Subject。四个尺度只有两种接缝。

### 接缝 A —— 进程（repo 用）

把 Subject 当黑盒程序跑，观察四样东西：

```
退出码 │ stdout │ stderr │ 跑完之后目录变成什么样
```

任何语言写的任何程序都有这四样，所以框架里**不需要一行按语言分支的代码**。

### 接缝 B —— 函数调用（kernel / module / package 用）

直接调用 Subject，喂参数，比返回值。一行一条 JSON：

```
→ {"call": "pairwise_distances", "args": [[...], [...]]}
← {"ok": true, "value": [[...]]}
```

解题者可以用任何语言实现，只要它能说这个协议。

### 为什么不能硬合成一种

- 用**接缝 A** 判一个函数：得先给它套个命令行外壳 —— 那你判的是外壳，不是函数。
- 用**接缝 B** 判一个仓库：得先给「仓库」指定一个入口函数，而这需要按语言写解析器。
  旧链路实测过：758 行代码只覆盖了两种语言，这条路会把「支持任意语言」这个前提废掉。

**所以观测层就是两个。这是本质差异，不是没对齐。** 除观测层之外，其余全部共用。

---

## 2. kernel 不是新范式

kernel = **module + 三样东西**，其中只有第三样是真缺的：

1. 输入词表要能表达「数组 + dtype + shape」
2. 比较器要用**浮点容差**而不是精确相等
3. 计时和执行环境要能上 GPU ← **只有这个是新的**

依据：旧 module 链路已经产出过 kernel 形状的题（成对距离、卷积的 col2im、朴素矩阵乘），
它们的输入 schema 就是 `float_array / float64 / size 500`，判等走浮点包络。
这套机器只有数值例程才需要，说明接缝 B 本来就装得下 kernel。

**本期只做 CPU。** kernel 作为 module 的一个 profile 存在，GPU 挂钩预留但不点亮：

- Harbor 原生支持 GPU（`task.toml` 里有 `gpus` 和 `gpu_types` 字段），接口照它的字段留，不自己发明。
- CPU 上的 kernel 题本身就成立：把数值例程向量化、换算法、消掉临时数组，都是 CPU 上可测的真加速。

---

## 3. 链路：七个环节

四个尺度走**同一条**链路。差别只在每个环节里插的插件不同。

```
① SOURCE      去哪找素材
② SPECIFY     确定改造什么、怎么调用它
③ BUILD       把参考实现真正建出来
④ PROBE       造一批输入
⑤ FREEZE      跑 N 遍，只有每遍都一致的才算数 → Expectation
⑥ EVIDENCE    检验 Verifier 自身是否诚实
⑦ EMIT        打包，然后用包里的参考驱动包里的 Verifier 再验一次
```

| 环节 | 统一的部分 | 按尺度分叉的部分 |
|---|---|---|
| ① SOURCE | GitHub 检索、去重记忆、按语言/星数分层采样 | **找什么**：仓库 / 包 / PR / 源码函数 |
| ② SPECIFY | 模型起草 → 返回 **JSON** → 逐字段校验组装 | 描述「怎么建仓库」还是「哪个符号 + 输入 schema」 |
| ③ BUILD | 容器内构建、按需装工具链、断网 | 无差别 |
| ④ PROBE | 变异引导搜索区分输入 | **输入词表宽度**（三档，见 §4） |
| ⑤ FREEZE | 跑 N 遍取交集；不可复现的**按位置遮蔽** | **观测层**：接缝 A / 接缝 B |
| ⑥ EVIDENCE | 同一套证据框架 | **比较器严格度**（三档，见 §4） |
| ⑦ EMIT | Harbor 包、`/app/run.sh`、`REWARD_PATH` | `task.toml` 的 `gpus` 字段 |

**真正按尺度分叉的只有四个插件点**：找什么、输入词表、观测层、比较器。骨架完全一样。

> **SPECIFY 永远只接受 JSON，绝不 exec 模型生成的代码。**
> 起草发生在任何容器存在之前，跑在工厂主机上。模型只能填写 Recipe 表达得了的字段，
> 也就意味着它无法发明一种新机制。它提议的构建步骤确实会执行，但那是稍后、在容器里。

---

## 4. 三档插件（按尺度递增，不是四套代码）

### 输入词表

| 档 | 能表达 | 谁用 |
|---|---|---|
| 标量 | int / float / str / bytes / bool / list / map / json | module |
| 数值 | 上一档 + array + dtype + shape + 复数 | kernel |
| 契约面 | 上一档 + 多个入口的分派 | package |

### 比较器

| 档 | 判等方式 | 谁用 |
|---|---|---|
| 精确 | 字节 / 哈希相等 | repo、module 的整数与字符串 |
| 结构 | JSON 等价（键序无关、数字按容差） | package |
| 数值包络 | 允许候选的误差不大于参考自身误差的 κ 倍 | kernel |

**数值包络为什么必须**：一个 GPU kernel 换了归约顺序，结果**必然**逐位不同却完全正确。
参考实现自己在 fp64 下的误差是尺子，候选只要不比它更差就算对。
这条让 kernel 不必假装浮点是精确的。

---

## 5. 已决的三件事（用实测定的，不是偏好）

### Q1 · Expectation 跑几遍 → **统一 N = 5**

98 个已出厂包中，**34% 曾被重复运行揪出不稳定**（某题 62 个场景不稳，另一题 48 个）。
若 N = 1，这些全都会被当成「可复现」冻进 Expectation，之后随机地判错正确的提交。
函数级现在只跑一遍；纯函数确实常常够用，但哈希序、时钟、并行输出顺序不会因为它是函数就消失。
代价只是多跑几遍。

### Q2 · 先打通哪个尺度 → **module → kernel(CPU) → package → repo**

module 是接缝 B 的最小实例。打通之后：kernel 只是加数值 profile，package 只是加多入口分派，
repo 走另一条接缝、且已被验证过。先做最小的那个，让四个插件点在真实题目上各被穿一次。

### Q3 · 速度门形态 → **统一到「置信区间下界 + 噪声地板 + 最差 shape」**

本机实测：把同一个程序和它自己比，真实加速比恒等于 1.000，八次测得
`1.006 1.026 0.684 1.260 1.029 1.018 1.011 0.969`，标准差 0.146。
按「log 比值映射到 [0.15, 1]」的旧曲线打分，一个**什么都没改**的提交能拿到 0.150 ~ 0.433 ——
也就是白拿了通往 2× 目标的 **33%**。这不是理论风险，是这台机器上量出来的。

所以速度必须这样测：
- 同一次运行里**交替**跑参考和候选，且轮换先后顺序（抵消漂移）
- 每次计时**重新抽输入**（防止缓存）
- 先用「参考 vs 参考」标定噪声地板
- 取自助法置信区间的**下界**，且必须同时越过噪声地板和有意义阈值
- 多个 shape 取**最差**的那个，杜绝只调优一个友好尺寸

---

## 6. 必须带过去的硬经验

以下每条都是已经发生过的故障，不是假想。地基里就要有。

1. **出厂之后，必须用包里的参考驱动包里的 Verifier 再验一次。**
   实测 78 个已出厂包中 **14 个的参考拿不到自己的满分**，且它们当时全部「认证通过」——
   因为所有闸门量的都是构建工作区，没有一个打开过真正发出去的包。

2. **零分是二义的。** 「跑不起来」和「答错了」都会得零分，任何检验都必须能区分这两者，
   否则它就是橡皮章。同理，变异体拿满分也是二义的：要么 Verifier 瞎了，要么变异根本没碰到计分项 ——
   必须直接 diff 观测来区分，不能从分数反推。

3. **不可复现的行按位置遮蔽，不能整行丢弃。** 丢整行等于允许解题者删掉那行照样满分。
   遮蔽必须先比对行数，否则删一行会把内容顶到被遮蔽的位置藏起来。

4. **同语言题必须有真实计时。** 参考实现就在解题者手里，没有速度维度的话 `cp` 一下就是满分。
   而且要先判 **headroom**：如果全部开销都是进程启动，放大工作量只会让数字变大、比值恒为 1，
   那是伪造出来的门。判据是最重与最轻探测的耗时之比。

5. **计时用的输入必须从计分集里扣除。** 否则可以在正确性阶段把结果缓存下来，计时阶段回放。

6. **不能把可运行的参考实现发给解题者。** `exec` 一下就满分，判分器无从分辨。

7. **凭据只有一个来源。** 环境变量优先，其次 `.env`，由唯一一个函数读取。
   凭据进容器走**环境变量**，不推文件 —— 推文件会把密钥写到沙箱磁盘上，还可能随产物回流。

---

## 7. 目录结构

```
frontier-refactor-factory/
  frf/
    core/                 四个尺度共用
      llm.py                模型网关；唯一读取凭据的地方
      sandbox.py            容器：本地 Docker / 远程微虚机 / GPU
      harbor.py             出厂格式（唯一一份）
      scoring.py            正确性是闸，速度是坡
      freeze.py             跑 N 遍取交集 → Expectation
      evidence.py           证据框架：每条闸都必须自证会咬人
      sourcing.py           检索 + 去重记忆

    observe/              两种接缝
      process.py            四通道
      call.py               JSON 行协议
      probes/               输入词表（三档）
      compare/              比较器（三档）

    scales/               每个尺度只写它独有的那点
      repo.py               从仓库自身的测试里采集命令序列
      package.py            安装并反射公开契约面
      module.py             挖 PR / 扫源码定位函数
      kernel.py             module + 数值 profile + GPU 挂钩

  tools/                  每个工具一个入口，文档字符串里给出可直接运行的命令
  tests/                  只跑真实进程；mock 出来的测试测的是错的东西
  DESIGN.md
```

**代码规范**：全新编写，不出现任何旧工厂的名字或缩写。
模块文档解释**为什么这样设计**，而不是复述代码做了什么；每道闸门的注释写明它拦的是哪一类错误。

---

## 8. 尚未决定

- **SOURCE 的产能**：repo 尺度实测每仓库良率约 9%，跨语言约 7%、同语言约 1.4%
  （同语言更难，因为「测试天生跑得快」导致多数语料没有可测的 headroom）。
  module / package / kernel 的良率尚无同口径数据，要在打通后立刻测，
  因为它决定四个尺度各自需要多大的素材池。
- **GPU 点亮时机**：接口按 Harbor 字段预留，何时接真卡另议。
- **难度分布**：目前只有「能不能出厂」这个二元判据，缺少「这题有多难」的刻度。
