# frontier-refactor-factory — 设计方案

> 状态：**方案讨论中，尚未实现**。本文件是动手前要先谈拢的东西。
>
> 目标：一条链路，产出四个尺度的重构任务 —— **kernel / module / package / repo**。
> 能统一的统一，必须分叉的分叉，但每一处分叉都要说得出「为什么它不能统一」。

---

## 0. 术语（先把话说清楚）

旧链路里的黑话各不相同，这里定死一套。

| 术语 | 含义 |
|---|---|
| **Subject** | 被改造的东西：一个计算例程 / 一个函数 / 一个包 / 一整个仓库 |
| **Probe** | 一次探测：喂给 Subject 的一组输入 |
| **Expectation** | 正确行为的记录。**测出来的，不是人写的** |
| **Verifier** | 判分器：跑候选 → 比对 Expectation → 给分 |
| **Evidence** | 出厂前对 **Verifier 自身** 的检验 |
| **Headroom** | 这道题究竟有没有可赚的优化空间 |

**Verifier 和 Evidence 的区别是本设计最重要的一条，务必分清：**

- **Verifier 判「提交对不对」** —— 面向解题者
- **Evidence 判「Verifier 自己诚不诚实」** —— 面向我们

一个永远给零分的 Verifier，能通过所有「错误提交必须得零分」的检验。所以 Evidence 必须
**两头都咬**：参考实现必须拿满分，胡乱提交必须得零分。缺任何一头，另一头都不成立。

---

## 1. 四个尺度，两种接缝

要判断「改写后行为变没变」，必须在某条**接缝**上观察 Subject。四个尺度只有两种接缝。

### 接缝 A —— 进程（repo 用）

把 Subject 当黑盒程序跑，观察四样东西：

```
退出码 │ stdout │ stderr │ 跑完之后目录变成什么样
```

任何语言写的任何程序都有这四样，所以框架里**不需要一行按语言分支的代码**。

### 接缝 B —— 函数调用（kernel / module / package 用）

直接调用 Subject，喂参数，比返回值。一行一条 JSON：

```
→ {"call": "pairwise_distances", "args": [[...], [...]]}
← {"ok": true, "value": [[...]]}
```

解题者可以用任何语言实现，只要它能说这个协议。

### 为什么不能硬合成一种

- 用**接缝 A** 判一个函数：得先给它套个命令行外壳 —— 那你判的是外壳，不是函数。
- 用**接缝 B** 判一个仓库：得先给「仓库」指定一个入口函数，而这需要按语言写解析器。
  旧链路实测过：758 行代码只覆盖了两种语言，这条路会把「支持任意语言」这个前提废掉。

**所以观测层就是两个。这是本质差异，不是没对齐。** 除观测层之外，其余全部共用。

---

## 2. kernel 不是新范式

kernel = **module + 三样东西**，其中只有第三样是真缺的：

1. 输入词表要能表达「数组 + dtype + shape」
2. 比较器要用**浮点容差**而不是精确相等
3. 计时和执行环境要能上 GPU ← **只有这个是新的**

依据：旧 module 链路已经产出过 kernel 形状的题（成对距离、卷积的 col2im、朴素矩阵乘），
它们的输入 schema 就是 `float_array / float64 / size 500`，判等走浮点包络。
这套机器只有数值例程才需要，说明接缝 B 本来就装得下 kernel。

**本期只做 CPU。** kernel 作为 module 的一个 profile 存在，GPU 挂钩预留但不点亮：

- Harbor 原生支持 GPU（`task.toml` 里有 `gpus` 和 `gpu_types` 字段），接口照它的字段留，不自己发明。
- CPU 上的 kernel 题本身就成立：把数值例程向量化、换算法、消掉临时数组，都是 CPU 上可测的真加速。

---

## 3. 链路：八个环节

四个尺度走**同一条**链路。差别只在每个环节里插的插件不同。

```
① SOURCE      去哪找素材
② SPECIFY     确定改造什么、怎么调用它
③ BUILD       把参考实现真正建出来
④ PROBE       造一批输入
⑤ FREEZE      跑 N 遍，只有每遍都一致的才算数 → Expectation
⑥ ADEQUACY    这批输入够不够？够不到的地方，补
⑦ EVIDENCE    检验 Verifier 自身是否诚实
⑧ EMIT        打包，然后用包里的参考驱动包里的 Verifier 再验一次
```

**⑥ 和 ⑦ 是两把不同的尺子，必须分开数**（这是最容易混的一处）：

- **EVIDENCE 问「这把尺子准不准」** —— Verifier 会不会误判
- **ADEQUACY 问「你量对地方了没」** —— 就算尺子完全准，你有没有量到程序真正在做的事

一个只含 `git --version` 的语料能通过**全部** Evidence：参考能复现、空提交得零分、
每条通道都咬人。但它什么都没考。**Evidence 全绿和题有价值是两件事。**

| 环节 | 统一的部分 | 按尺度分叉的部分 |
|---|---|---|
| ① SOURCE | GitHub 检索、去重记忆、按语言/星数分层采样 | **找什么**：仓库 / 包 / PR / 源码函数 |
| ② SPECIFY | 模型起草 → 声明部分收 JSON、生成器只在容器跑 | 描述「怎么建仓库」还是「哪个符号 + 输入 schema」 |
| ③ BUILD | 容器内构建、按需装工具链、断网 | 无差别 |
| ④ PROBE | 变异引导搜索区分输入 | **探测怎么来**：schema 采样 / 生成器（见 §4） |
| ⑤ FREEZE | 跑 N 遍取交集 | **不稳定怎么处理**：遮蔽 / 丢弃（见 §5 Q1） |
| ⑥ ADEQUACY | 行覆盖率测量 + 暗区补探测的循环 | 测谁的覆盖率：整个程序 / 被测符号 |
| ⑦ EVIDENCE | 同一套证据框架 | **比较器严格度**（三档，见 §4） |
| ⑧ EMIT | Harbor 包、`/app/run.sh`、`REWARD_PATH` | `task.toml` 的 `gpus` 字段 |

**真正按尺度分叉的只有四个插件点**：找什么、探测怎么来、观测层、比较器。骨架完全一样。

### SPECIFY 里模型能写什么、不能写什么

这里有一处真实的张力，必须写明白，否则实现时会各做各的。

**规则：模型的输出永远不在工厂主机上执行；要执行，只能在容器里。**

起草发生在任何容器存在之前。所以：

- **声明性的部分（构建步骤、调用方式、输入 schema）→ 只收 JSON**，逐字段校验后组装。
  模型只能填 Recipe 表达得了的字段，因此**它无法发明一种新机制**。
  这些构建步骤当然会执行 —— 但那是稍后，在容器里，和一份手写 Recipe 的风险完全相同。

- **生成性的部分（探测输入的生成器）→ 允许模型写代码，但只在容器里跑。**
  这是必要的：一个包的契约面有几十个入口，各自的合法输入形态不同
  （"一个词干还原器要吃真实单词"、"一个 cron 解析器要吃 cron 表达式"），
  纯 schema 表达不出来。实测走的就是这条路 —— 90 个包的探测生成器都是模型写的，不是人写的。

**两者的安全边界不同，所以在代码里也必须是两条路径**：JSON 走主机侧的校验器，
生成器走容器侧的执行器。把它们混成一条，等于让主机 exec 模型输出。

### EMIT 只有一份，这点是查过的

"出厂格式共用"听起来像口号，所以我把两个接缝已有的产物逐字段比了一遍：

- **`task.toml` 22 个字段里 17 个完全一致** —— 连 `scale` 字段都已经各自写上了。
  分歧只有 5 个，其中 `source_language` / `target_language` 对两个接缝**都有意义**，
  只是其中一边没填；剩下的是超时和 `license`。
- **判分结果的两个核心字段一模一样**：`reward`（浮点）和 `speedup`。
  唯一的差别是"过了几个/共几个"那一对的**命名**不同（一边叫 points 一边叫 cases）。

所以统一 EMIT 不是重写，是**取并集 + 统一命名**。这条结论支撑了 §10 的接口划分：
出厂格式属于 `core/`，四个尺度都不该碰它。

---

## 4. 按尺度分叉的两个插件点

### 探测怎么来（两种，不是三档）

这里我原本写成"三档递增"，查过实现之后发现是**两种不同的机制**，不是一条线上的深浅：

| 机制 | 怎么造输入 | 谁用 | 为什么 |
|---|---|---|---|
| **Schema 采样** | 声明式 schema → 采样器按 kind/dtype/shape 抽 | module、kernel | 单个函数的入参类型是固定的，声明得出来 |
| **生成器** | 模型写一个 `probes(n)` 生成器，容器里跑 | package | 契约面有几十个入口，每个的合法输入形态不同 |

**为什么 package 不能用 schema**：一个包的公开面是几十个函数，
`soundex("wagner")` 和 `porter("nationalizational")` 的合法输入形态完全不同，
还得按真实比例混合、并且必须包含**会被拒绝的**输入（错误路径也是契约的一部分）。
把这个塞进声明式 schema，等于重新发明一门语言。

**共用的是下游**：无论输入怎么来，之后的冻结、比对、计时、证据全部同一套。
分叉只在"怎么造出这批输入"这一步。

kernel 相对 module 的增量是在 **schema 的词表里**加数组：`array + dtype + shape + 复数`。
这是同一个机制的扩充，不是新机制。

### 比较器

| 档 | 判等方式 | 谁用 |
|---|---|---|
| 精确 | 字节 / 哈希相等 | repo、module 的整数与字符串 |
| 结构 | JSON 等价（键序无关、数字按容差） | package |
| 数值包络 | 允许候选的误差不大于参考自身误差的 κ 倍 | kernel |

**数值包络为什么必须**：一个 GPU kernel 换了归约顺序，结果**必然**逐位不同却完全正确。
参考实现自己在 fp64 下的误差是尺子，候选只要不比它更差就算对。
这条让 kernel 不必假装浮点是精确的。

---

## 5. 已决的三件事（用实测定的，不是偏好）

### Q1 · Expectation 跑几遍 → **统一 N = 5，但处理不稳定的方式两个接缝不同**

98 个已出厂包中，**34% 曾被重复运行揪出不稳定**（某题 62 个场景不稳，另一题 48 个）。
若 N = 1，这些全都会被当成「可复现」冻进 Expectation，之后随机地判错正确的提交。
函数级现在只跑一遍；纯函数确实常常够用，但哈希序、时钟、并行输出顺序不会因为它是函数就消失。
代价只是多跑几遍。

**但"跑 N 遍之后拿它怎么办"，两个接缝的答案必须不同**，这是查过实现才发现的：

| | 观测是什么 | 不稳定时怎么办 |
|---|---|---|
| 接缝 A | 有序的**行序列** | **按位置遮蔽**：第 7 行每次都变 → 只把第 7 行排除，其余照常计分 |
| 接缝 B | 一棵**返回值树** | **整条探测丢弃**：树没有"第 7 行"这种坐标，遮蔽无从下手 |

按位置遮蔽依赖"行号是稳定坐标"这个前提，而返回值树里没有对应物。
硬把树拍平成行去遮蔽会引入一个假坐标系 —— 字段顺序一变，遮蔽的位置就指向了别的东西。

所以接缝 B 的规矩是：**一条探测只要在 N 次里不完全一致，就整条丢掉**，
并且**丢掉的比例要报出来**（丢太多说明这个 Subject 根本不确定，该拒绝而不是缩小语料）。
代价是接缝 B 的语料会因为不稳定而变小；好处是它不会伪造一个坐标系。

> 现有实现里这一条是缺的：接缝 B 的参考**只跑一遍**就冻结，
> 靠事后"换 5 个哈希种子重跑必须完全一致"来兜底。那是**拒绝**不稳定，不是**测量**它 ——
> 而且只覆盖哈希序这一种来源，时钟和并行顺序照样漏。本链路把它并进统一的 N 遍冻结。

### Q2 · 先打通哪个尺度 → **module → kernel(CPU) → package → repo**

module 是接缝 B 的最小实例。打通之后：kernel 只是加数值 profile，package 只是加多入口分派，
repo 走另一条接缝、且已被验证过。先做最小的那个，让四个插件点在真实题目上各被穿一次。

### Q3 · 速度门形态 → **统一到「置信区间下界 + 噪声地板 + 最差 shape」**

本机实测：把同一个程序和它自己比，真实加速比恒等于 1.000，八次测得
`1.006 1.026 0.684 1.260 1.029 1.018 1.011 0.969`，标准差 0.146。
按「log 比值映射到 [0.15, 1]」的旧曲线打分，一个**什么都没改**的提交能拿到 0.150 ~ 0.433 ——
也就是白拿了通往 2× 目标的 **33%**。这不是理论风险，是这台机器上量出来的。

所以速度必须这样测：
- 同一次运行里**交替**跑参考和候选，且轮换先后顺序（抵消漂移）
- 每次计时**重新抽输入**（防止缓存）
- 先用「参考 vs 参考」标定噪声地板
- 取自助法置信区间的**下界**，且必须同时越过噪声地板和有意义阈值
- 多个 shape 取**最差**的那个，杜绝只调优一个友好尺寸

---

## 6. Adequacy：这批探测够不够

Evidence 保证尺子准，**Adequacy 保证你量对了地方**。两者正交，缺哪个都出废题：

- Evidence 全绿、Adequacy 差 → 题是"健全的废题"：判分完全公正，但什么都没考
- Adequacy 好、Evidence 差 → 题覆盖得很全，但判分本身会误判

**判据实验**：一个只含 `git --version` 的语料能通过全部八条 Evidence ——
参考能复现它、空提交得零分、每条通道都咬人。而一份三行的假实现能拿满分。

### 机制：覆盖率 → 暗区 → 补探测，循环

```
1. 让参考带着覆盖率跑一遍现有语料      → 哪些行执行了(LIVE)、哪些没有(DARK)
2. 把 DARK 行按所属函数聚合，挑基本没碰到的
3. 问模型：这些暗函数里，哪些是契约承诺要判的？(而不是时区/无关选项这类边角)
   对承诺要判的，让它提出具体的新输入
4. 新输入并进语料，重新冻结，回到 1
```

**为什么第 3 步需要模型**：暗区不等于该补。一个包里没被执行的代码，
可能是它承诺的功能（该补），也可能是无关的边角（不该补，补了只是撑大语料）。
这个判断需要读懂契约，是模型该干的活 —— 但**它只提议输入，不裁定对错**，
新输入的正确答案照样由参考跑出来。

### 两个可测的数，缺一不可

| 数 | 问什么 | 为什么单独看会骗人 |
|---|---|---|
| **REACH** 行覆盖率 | 语料执行到了被测物的多少行 | 高覆盖率不等于能分辨对错：执行到了但观测不到，照样是暗的 |
| **FLOOR** 白送率 | 一个什么都不做的提交已经能拿多少分 | 低白送率不等于覆盖广：可能只是难蒙，但只考了一小块 |

**FLOOR 必须试多种平凡提交取最大值**，只试一种等于看运气 ——
实测同一道题上 `exit 0` 得 50%、`exit 1` 得 75%。

### 按尺度分叉的只有"测谁的覆盖率"

- **repo**：整个程序的行覆盖率。分母大得吓人（一个大项目十几万行可执行行），
  所以**只能当低门槛用**，用来拒"几乎没碰到程序"的语料，不能当作充分性的证明。
- **kernel / module**：被测符号自己的行。分母小，这个数才真正有意义。
- **package**：契约面上每个入口的行。还要额外看**入口覆盖**——
  有没有哪个公开函数一次都没被调用过。

---

## 7. Evidence：判分器自己要过的关

Verifier 判提交，**Evidence 判 Verifier**。这一组是四个尺度共用的，因为它问的是
"这把尺子准不准"，而尺子准不准跟量的是仓库还是函数无关。

三条来自三个不同尺度的实战，各自堵的洞不一样，**取并集而不是取交集**：

| | 问什么 | 不加会怎样 | 适用 |
|---|---|---|---|
| **E1 天花板** | 参考实现必须拿满分 | 一个永远给 0 分的判分器能通过所有"坏提交得零分"的检查 | 全部 |
| **E2 地板** | 什么都不做必须得零分 | 判分器可能根本没在比 | 全部 |
| **E3 每条通道都咬人** | 每个计分通道单独扰动一次，都要被抓到 | 号称计分的通道其实是常数 | 全部 |
| **E4 不发可运行的参考** | 解题者拿到的树里不能有能跑的参考 | `exec /reference/x` 就满分 | 全部 |
| **E7 出厂包自证** | 用包里的参考驱动包里的判分器，必须满分 | **实测 14/78 个已出厂包栽在这条** | 全部 |
| **E8 种子无关** | 换哈希种子重跑，Expectation 必须一致 | 冻进去的其实是这次运行的字典序 | 全部 |
| **E5 计分点关于被测物** | 计分点不能落在从不调用被测物的步骤上 | 判的是包装的 shell，不是程序 | **仅接缝 A** |
| **E6 反作弊** | 候选不能把活转包给原实现 | 跨语言题里"重实现"变成"转发" | 全部，但**两种做法** |

**E5 只有接缝 A 需要，这不是偷懒。** 进程级的一个步骤可以是纯 shell、从不调用被测程序，
那样计分点判的是宿主的 `/bin/sh`。而接缝 B 的每一个观测**按构造**都来自 `entry(args)` 的返回值 ——
不调用被测物就没有观测，所以这个洞在结构上不存在。
**一个尺度不需要某条 Evidence，必须说得出"为什么它在结构上不可能发生"**，
说不出就是漏了而不是不适用。

**E6 两个接缝的做法完全不同，这点必须写清楚**：
- **接缝 A**：判分器只看四通道，**无法得知二进制是什么语言编的**。
  唯一可执行的手段是**把源语言工具链从镜像里删掉**，让"用原语言重建"做不到。
  注意光从 apt 列表减包无效 —— 基础镜像可能已经把工具链装在 `/usr/local` 下，必须按路径删。
- **接缝 B**：候选是个进程，能检测它有没有 import/链接原库。这是**检查**，不是**剥夺能力**。

两者的强度不一样：接缝 A 是"做不到"，接缝 B 是"做了会被发现"。设计上要接受这个差别，
而不是假装有一个统一的反作弊。

**E3 的关键细节（这条最容易做成橡皮章）**：变异体拿满分是**二义**的 ——
要么判分器瞎了（致命），要么变异根本没碰到计分项（无意义）。
只看分数分不出这两者的证明**本身就是橡皮章**。所以 E3 不能从分数反推：
必须把参考和变异体的观测直接 diff，**只有可证发散的才算证据**；
没发散的报"惰性"，全惰性则判"不确定"而不是通过。

**E1 和 E2 必须成对**，这是整组的骨架：只有 E2 会被"全判零分"的坏判分器通过，
只有 E1 会被"全判满分"的通过。两头都咬，中间才有意义。

### E4 有个容易漏的半边：判分器自己的目录

E4 通常被实现成"检查发给解题者的那棵参考树"。**这只是一半。**
判分器自己的目录里装着可运行的参考、Expectation、和全部探测输入 ——
如果判分器跑在**解题者的同一个环境里**，那个目录就是可读的。

出厂描述文件里有个字段决定这件事（Harbor 的 `environment_mode`）：

- `separate` —— 判分器在独立环境跑，解题者读不到
- `shared` —— 判分器在解题者的环境里跑，`/tests` **对提交可读**

写这份方案时我抽查了一遍现有产出，发现**同一个组织的两个尺度选了相反的值**：
仓库尺度 7/7 是 `separate`，且注释写明了原因；包尺度 **91/91 是 `shared`**，
而它的 `cases.jsonl` 每一行都是 `{"args": [...], "result": <正确答案>}` —— 明文答案。
在那种模式下，一份读文件并回放的提交可以拿满分，而题面里"不要调用参考实现"
就只是一句请求，不是一条性质。

**所以本链路的规定是：`separate` 是默认且唯一，Expectation 只存哈希不存明文。**
E4 必须同时检查两个目录，缺一个就等于没检查。

---

## 8. 必须带过去的硬经验

上面那八条 Evidence 是**闸门**，出厂时逐条跑。这一节是**写代码时的约束** ——
它们不是检查项，是一旦写错就没有闸门能补救的东西。每条都已经发生过。

1. **不可复现的行按位置遮蔽，不能整行丢弃。**
   丢整行等于允许解题者删掉那行照样满分。遮蔽必须**先比对行数**，
   否则删一行会把后面的内容顶到被遮蔽的位置藏起来。

2. **同语言题必须有真实计时，而且要先判 headroom。**
   参考实现就在解题者手里，没有速度维度的话 `cp` 一下就是满分。
   但"有计时"不等于"有意义"：如果全部开销都是进程启动，放大工作量只会让数字变大、
   比值恒为 1，那是伪造出来的门。判据是**最重与最轻探测的耗时之比** ——
   比值接近 1 说明这份语料量的是启动，不是活。

3. **计时用的输入必须从计分集里扣除。**
   否则可以在正确性阶段把结果缓存下来，计时阶段回放，拿满分加任意加速比。

4. **凭据只有一个来源。** 环境变量优先，其次 `.env`，由唯一一个函数读取。
   凭据进容器走**环境变量**，不推文件 —— 推文件会把密钥写到沙箱磁盘上，还可能随产物回流。

5. **"零分"和"跑不起来"必须能分开。**
   这条贯穿全链路而不只是判分：一个失败原因如果把"这仓库不适合"和"我们的工具坏了"
   归成一类，修复循环就会把工具的 bug 当成素材的问题，一直修错地方。

---

## 9. 目录结构

```
frontier-refactor-factory/
  frf/
    core/                 完全不知道观测长什么样 —— 只经手 Observation 这个抽象
      llm.py                模型网关；唯一读取凭据的地方
      sandbox.py            容器：本地 Docker / 远程微虚机 / GPU
      harbor.py             出厂格式（唯一一份）
      scoring.py            正确性是闸，速度是坡
      evidence.py           证据框架：每道闸都必须自证会咬人
      sourcing.py           可枚举来源的检索 + 去重记忆
      adequacy.py           覆盖率 → 暗区 → 补探测的循环
      pipeline.py           八个环节的驱动；调 Observer，不拆开看它

    observe/              知道观测长什么样 —— 两个接缝各自完整
      process/              接缝 A：四通道
        observation.py        exit / stdout / stderr / tree
        freeze.py             跑 N 遍 → 按位置遮蔽不稳定的行
      call/                 接缝 B：JSON 行协议
        observation.py        返回值树（或抛出的错误）
        freeze.py             跑 N 遍 → 整条丢弃不稳定的探测
      probes/               探测来源：schema 采样 / 容器里跑的生成器
      compare/              比较器（精确 / 结构 / 数值包络）

    scales/               每个尺度只写它独有的那点
      repo.py               从仓库自身的测试里采集命令序列
      package.py            安装并反射公开契约面
      module.py             挖 PR / 扫源码定位函数
      kernel.py             module + 数值 profile + GPU 挂钩

  tools/                  每个工具一个入口，文档字符串里给出可直接运行的命令
  tests/                  只跑真实进程；mock 出来的测试测的是错的东西
  DESIGN.md
```

**代码规范**：全新编写，不出现任何旧工厂的名字或缩写。
模块文档解释**为什么这样设计**，而不是复述代码做了什么；每道闸门的注释写明它拦的是哪一类错误。

---

## 10. 接口：一个尺度到底要写多少代码

上面说「共用」「分叉」都还是话。落到接口上，一个新尺度需要且只需要提供四样东西：

```python
class Scale(Protocol):
    """一个尺度 = 四个回答。链路的八个环节由框架驱动，尺度只回答这四问。"""

    def find(self, budget: int) -> Iterable[Candidate]:
        """去哪找素材。仓库 / 包 / PR / 源码函数。"""

    def specify(self, candidate: Candidate) -> Spec:
        """这一个候选要怎么建、怎么调用。声明性部分必须能被逐字段校验。"""

    def observe(self) -> Observer:
        """接缝 A 还是接缝 B。返回 process.Observer 或 call.Observer。"""

    def probes(self, spec: Spec) -> ProbeSource:
        """探测从哪来。schema 采样器，或容器里跑的生成器。"""
```

框架负责的部分**对四个尺度逐字相同**：建环境、跑 N 遍冻结、补足充分性、搜索区分输入、
扣除计时探测、跑证据组、打 Harbor 包、驱动出厂包复验。

**判据**：如果实现一个新尺度需要改 `core/` 里的任何一行，那说明这份抽象错了 ——
要么这个尺度不属于这四个，要么某个"共用"的东西其实不共用。这条判据比任何文档都硬，
所以它写在这里，将来第一个新尺度就是对它的检验。

### 这条判据当场就抓到了一个错误的划分

我拿现有实现验了一遍"哪些代码真的与观测无关"，数了它们提到四通道的次数：

| 模块 | 提到 `stdout/stderr/exit_code/tree` |
|---|---|
| 打分（正确性当闸、速度当坡） | **0 次** ← 真的通用 |
| 哈希与路径清洗 | 2 次 ← 基本通用 |
| 数据类型定义 | 18 次 |
| 冻结与判分 | 28 次 |
| 执行器 | 15 次 |

也就是说：**"跑 N 遍冻结"这件事是通用的，但现有的那份实现不是** ——
它的类型里直接写死了 `argv` / `stdout` / `tree` 这些字段，而接缝 B 的一次观测
既没有 argv 也没有目录树。照搬过去就等于把四通道塞进函数调用，
那正是 §1 说不能做的事。

**所以 `core/` 的边界要按"依赖不依赖观测形状"来划，不是按"听起来通用不通用"：**

```
core/   完全不知道观测长什么样：打分、证据框架、容器、出厂格式、检索
        ↑ 只处理 Observation 这个抽象，从不拆开看里面

observe/  知道观测长什么样：两个接缝各自的 Observation 类型、
          各自的冻结与比较实现
```

冻结算法（跑 N 遍、取一致的、其余按接缝各自处理）写在 `observe/` 的两个实现里，
而不是 `core/`。**这不是重复代码 —— 是同一个思想在两种数据形状上的两次落地**，
硬合的代价是发明一个假的公共表示。

### 两个接缝的接口

```python
class Observer(Protocol):
    def run(self, subject: Subject, probe: Probe) -> Observation: ...
    def compare(self, expected: Observation, actual: Observation) -> Verdict: ...
```

- `process.Observer` 的 `Observation` 是四通道的哈希 + 行数 + 遮蔽位置
- `call.Observer` 的 `Observation` 是返回值树（或抛出的错误）

**判分器只依赖 `Observer` 这两个方法**，所以「加一个尺度」和「加一种观测」是两件独立的事。

---

## 11. 尚未决定

- **SOURCE 的产能，四个尺度差一个数量级，而这决定素材池要多大。**

  | 尺度 | 实测 | 备注 |
  |---|---|---|
  | repo 跨语言 | 每仓库 ~7% | 走过 503 个仓库的统计 |
  | repo 同语言 | 每仓库 ~1.4% | 「测试天生跑得快」→ 多数语料没有可测的 headroom |
  | package | 已交付 91 道 | 但**每道题的探测生成器是模型现写的**，成本在起草不在挖矿 |
  | module / kernel | **无同口径数据** | 打通后第一件事就是测它 |

  两种供给模型不一样，不能混着谈：repo 是**筛**（走一百个仓库留几个），
  package 是**写**（选定一个包，然后请模型把契约面写出来）。
  前者的瓶颈是池子大小，后者的瓶颈是起草质量。四个尺度各属哪一种，要在打通时确认。

  **而且连"去哪找"的机制都不同**，这点查实现才发现：
  - repo 尺度**检索索引**：`language:X stars:A..B archived:false`，按星数分段翻页，
    池子有多大是可查的，翻到哪一页也是可记的。
  - package 尺度**问模型要名字**：给几个领域，让模型列出它记得的包。
    这条路没有"池子大小"可言，产能上限是模型的记忆而不是索引的规模，
    而且**它不会告诉你它重复了或者漏了**。

  合并时这里必须统一到**可枚举的来源**（索引、registry、依赖图），
  模型可以用来**排序和筛选**候选，但不能用来**产生**候选清单 ——
  否则"还剩多少素材"这个问题永远答不出来，而那正是 scalability 的定义。
- **GPU 点亮时机**：接口按 Harbor 字段预留，何时接真卡另议。
- **难度分布**：目前只有「能不能出厂」这个二元判据，缺少「这题有多难」的刻度。
