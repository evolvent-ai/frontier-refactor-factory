# frontier-refactor-factory — 设计方案

> 一条链路，产出四个尺度的性能重构任务：**kernel / module / package / repo**。
> 能统一的统一，必须分叉的分叉，但**每一处分叉都要说得出「为什么它不能统一」**。
>
> 本文件是编码依据。凡与本文件冲突的实现，以本文件为准；凡本文件说不清的，
> 先改本文件再动代码。
>
> **实现进度**（2026-08-17）：脊柱与两个观测接缝已落地并测过 —— 八环节与闸门、
> 两种冻结、E1–E8 证据组、计分、计时、adequacy、sourcing、题面、出厂、call 接缝的
> 六个共享环节。**四个 scale 尚未实现**（`frf/scales/` 为空），
> process 接缝的共享环节、E2B backend、coverage 后端也还没写。
> §16 的里程碑是下一步。

---

## 0. 术语

旧链路各有各的黑话，这里定死一套，全链路只用这些词。

| 术语 | 含义 |
|---|---|
| **Subject** | 被改造的东西：一个计算例程 / 一个函数 / 一个包 / 一整个仓库 |
| **Probe** | 一次探测：喂给 Subject 的一组输入 |
| **Expectation** | 正确行为的记录。**测出来的，不是人写的** |
| **Verifier** | 判分器：跑候选 → 比对 Expectation → 给分 |
| **Evidence** | 出厂前对 **Verifier 自身** 的检验 |
| **Adequacy** | 这批 Probe 够不够 —— 有没有量到 Subject 真正在做的事 |

**三把尺子，互相正交，缺一不可：**

```
Verifier  判「提交对不对」        → 面向解题者
Evidence  判「Verifier 诚不诚实」  → 面向我们
Adequacy  判「你量对地方了没」    → 面向题目本身
```

**为什么 Verifier 和 Evidence 必须分开**：一个永远给零分的 Verifier，能通过所有
「错误提交必须得零分」的检查。所以 Evidence 必须**两头都咬** —— 参考实现必须拿满分（E1），
胡乱提交必须得零分（E2）。缺任何一头，另一头都不成立。

**为什么 Adequacy 不能并进 Evidence**：一个只含 `git --version` 的语料能通过**全部**
Evidence —— 参考能复现它、空提交得零分、每条通道都咬人。但它什么都没考。
**Evidence 全绿 ≠ 题有价值。**

---

## 1. 四个尺度，两种接缝

要判断「改写后行为变没变」，必须在某条**接缝**上观察 Subject。

**接缝 = 观察点的位置。** 两种接缝都是黑盒（都不读源码），区别只在观察点画在哪。

### 接缝 A —— 进程四通道（repo 用）

把 Subject 当黑盒程序跑，每条命令之后记录四样：

```
退出码 │ stdout │ stderr │ 跑完之后目录变成什么样
```

任何语言写的任何程序都有这四样，所以框架里**不需要一行按语言分支的代码**。

### 接缝 B —— 函数调用 JSON 行协议（kernel / module / package 用）

Subject 跑成一个独立进程，通过一行一条 JSON 与它对话：

```
→ {"id": 1, "op": "run",  "args": [...]}
← {"id": 1, "ok": true, "result": ...}
→ {"id": 2, "op": "time", "args": [...], "n": 1000}
← {"id": 2, "secs": 0.42, "err": false}
```

**协议本身与语言无关。** 候选侧只需要一个约 30 行的 shim，把本语言的函数接到这个协议上。

### 为什么不能硬合成一种

- 用**接缝 A** 判一个函数：得先给它套个命令行外壳 —— 那判的是外壳，不是函数。
- 用**接缝 B** 判一个仓库：得先给「仓库」命名一个入口函数，而那需要**按语言写解析器**。
  旧链路实测：为此写了 758 行，只支持两种语言。

**这是本质差别，不是没对齐。除观测层之外，其余全部共用。**

### 两个接缝都是独立进程

四个尺度**一律进程隔离**，接缝 B 不做同进程调用。三个理由，缺一条都不足以定案：

1. **任意语言的必要条件。** 同进程调用意味着判分器要能 import 候选 ——
   那就假设了候选与判分器同语言。
2. **同语言题唯一的防作弊手段。** 同语言题里参考实现就在解题者手里。同进程调用时
   `def entry(args): import reference; return reference.entry(args)` 正确性满分。
   只有进程隔离 + import 检查 + 受限账户（§8）能拦住。
3. **执行隔离的前提。** 计时时挂起对侧、限制进程数（§8），前提就是两边是独立进程。

**代价**：Probe 必须能 JSON 序列化。传自定义对象的函数需要在 shim 里构造，或被筛掉。
**这个代价的大小是未知数，必须在 module 打通时测出来**（§16），不能靠猜。

---

## 2. kernel 不是新范式

```
kernel = module + ① 数组词表 + ② 浮点容差 + ③ 可插拔成本函数
```

依据：旧 module 链路已经产出过 kernel 形状的题（成对距离、卷积的 col2im、朴素矩阵乘），
它们的输入 schema 就是 `float_array / float64 / size 500`，判等走浮点包络。
这套机器只有数值例程才需要 —— 说明接缝 B 本来就装得下 kernel。

### 成本函数可插拔，这比 GPU 重要

时间不是唯一的成本度量，而且它是**噪声最大**的那个：

| 成本度量 | 精确性 | 用在哪 |
|---|---|---|
| wall-clock（交替 + 现场标定噪声地板） | 有噪声，须统计 | 默认，全尺度 |
| **模拟器周期数** | **精确，零噪声** | 闭合模拟器类 kernel |
| 指令数 / 分配次数（perf counter、valgrind） | 近似精确 | CPU kernel |
| GPU event timing | 有噪声 | **留接口，本期不实现** |

**本期只做 CPU。** CPU 上的 kernel 题本身就成立：向量化、换算法、改内存布局、消除临时数组，
都是 CPU 上可测的真加速。GPU 按 Harbor 已有的字段（`gpus` / `gpu_types`）留接口，不自己发明。

**kernel 的范围不局限于「只改数据布局和指令顺序」** —— 任何有明确成本函数的计算例程都算。

---

## 3. 链路：八个环节

四个尺度走**同一条**链路。差别只在每个环节插的插件不同。

```
① SOURCE     去哪找素材            —— 只用可枚举索引
② SPECIFY    改造什么、怎么调用     —— 模型产出，逐字段校验
③ BUILD      把参考真正建出来       —— 容器内，断网
④ PROBE      造一批输入            —— schema 采样 / 生成器
⑤ FREEZE     跑 N=5 遍取交集 → Expectation
⑥ ADEQUACY   够不够？不够就补，循环
⑦ EVIDENCE   检验 Verifier 自身
⑧ EMIT       打包，再用包里的参考驱动包里的 Verifier 验一次
```

| 环节 | 统一 | 按尺度分叉 |
|---|---|---|
| ① SOURCE | 索引检索、去重记忆、分层采样 | **找什么**：仓库 / 包 / 源码函数 / PR |
| ② SPECIFY | 声明收 JSON、生成器只在容器跑 | 描述「怎么建仓库」还是「哪个符号 + 输入 schema」 |
| ③ BUILD | 容器内构建、按需装工具链、断网 | 无差别 |
| ④ PROBE | 变异引导搜索区分输入 | **探测怎么来**（§4） |
| ⑤ FREEZE | **跑 5 遍取交集** | **不稳定怎么处理**（§5） |
| ⑥ ADEQUACY | 覆盖率 → 暗区 → 补探测循环 | 测谁的覆盖率 |
| ⑦ EVIDENCE | 同一套证据组 | E5 仅接缝 A；E6 两种做法 |
| ⑧ EMIT | Harbor 包、`/app/run.sh`、`REWARD_PATH` | `task.toml` 的 `gpus` 字段 |

**真正分叉的只有四个插件点**：找什么、探测怎么来、观测层、比较器。骨架完全一样。

### 全链路在容器里跑

**默认 E2B 微虚机，backend 抽象保留本机 Docker 但不是默认。** 四个理由：

1. **Scalability** —— 本机核数是硬顶，沙箱可以横向铺开。
2. **本机 DinD 不可用** —— 无 `CAP_SYS_ADMIN`，容器证据一直靠沙箱起 daemon 才跑通。
3. **出题环境必须等于出厂环境** —— 在工厂主机冻结、发一个不含那套工具链的镜像，
   等于 Expectation 描述了一个解题者拿不到的程序。旧链路五个缺陷里四个是这一类。
4. **干净** —— 每次全新沙箱，不受并行批次干扰。

**推论：FREEZE 必须在任务镜像内进行**，不是在工厂主机上。

### SPECIFY 里模型能写什么、不能写什么

**规则：模型的输出永远不在工厂主机上执行；要执行，只能在容器里。**

- **声明性部分**（构建步骤、调用方式、输入 schema）→ **只收 JSON**，逐字段校验后组装。
  模型只能填 Recipe 表达得了的字段，因此**它无法发明一种新机制**。
  这些构建步骤当然会执行 —— 但那是稍后、在容器里，与一份手写 Recipe 的风险完全相同。

- **生成性部分**（探测输入的生成器）→ 允许模型写代码，**但只在容器里跑**。
  这是必要的：一个包的契约面有几十个入口，各自合法输入形态不同
  （词干还原器要吃真实单词，cron 解析器要吃 cron 表达式），纯 schema 表达不出来。

**两者的安全边界不同，在代码里必须是两条路径**：JSON 走主机侧校验器，生成器走容器侧执行器。
混成一条等于让主机 exec 模型输出。

### 模型在全链路里只做三件事

1. 起草 Recipe（收 JSON、逐字段校验）
2. 写探测生成器（只在容器里跑）
3. Adequacy 审计时提议补哪些输入（**内部用，不进题面**）

**三件事都不产生候选清单，也都不裁定对错。** 新探测的正确答案照样由参考跑出来。

---

## 4. 两个分叉点

### 4.1 探测怎么来

两种**不同的机制**，不是一条线上的深浅：

| 机制 | 怎么造输入 | 谁用 | 为什么 |
|---|---|---|---|
| **Schema 采样** | 声明式 schema → 按 kind/dtype/shape 采样 | module、kernel | 单个函数入参类型固定，声明得出来 |
| **生成器** | 模型写 `probes(n)`，容器里跑 | package | 契约面几十个入口，各自形态不同 |

**为什么 package 不能用 schema**：`soundex("wagner")` 和 `porter("nationalizational")`
的合法输入形态完全不同，还要按真实比例混合，并且**必须包含会被拒绝的输入**
（错误路径是契约的一部分）。塞进声明式 schema 等于重新发明一门语言。

**共用的是下游**：无论输入怎么来，之后的冻结、比对、计时、证据全部同一套。

kernel 相对 module 的增量在 **schema 词表**里：`array + dtype + shape + 复数`。
同一机制的扩充，不是新机制。

### 4.2 比较器

| 档 | 判等方式 | 谁用 |
|---|---|---|
| 精确 | 字节 / 哈希相等 | repo、module 的整数与字符串 |
| 结构 | JSON 等价（键序无关） | package |
| 数值包络 | 候选误差 ≤ 参考自身误差的 κ 倍 | kernel |

**数值包络为什么必须**：一个数值 kernel 换了归约顺序，结果**必然**逐位不同却完全正确。
参考实现自己在 fp64 下的误差是尺子，候选只要不比它更差就算对。
这条让 kernel 不必假装浮点是精确的。

---

## 5. FREEZE：跑 5 遍，两个接缝处理不稳定的方式不同

**统一 N = 5。** 实测：98 个已出厂包中 **34% 曾被重复运行揪出不稳定**。
若 N=1，这些全会被当成「可复现」冻进 Expectation，之后随机地判错正确的提交。
纯函数常常够用，但哈希序、时钟、并行输出顺序不会因为它是函数就消失。

**但「跑完 5 遍拿它怎么办」，两个接缝必须不同**：

| | 观测是什么 | 不稳定时 |
|---|---|---|
| 接缝 A | 有序的**行序列** | **按位置遮蔽**：第 7 行每次都变 → 只排除第 7 行，其余照常计分 |
| 接缝 B | 一棵**返回值树** | **整条 Probe 丢弃**，并报告丢弃率 |

按位置遮蔽依赖「行号是稳定坐标」，返回值树里没有对应物。
硬把树拍平成行去遮蔽会**发明一个假坐标系** —— 字段顺序一变，遮蔽位置就指向别的字段。

**这不是两套逻辑，是同一条原则的两次落地：**

> **只对能稳定复现的观测计分。有稳定坐标就精确到坐标，没有就整条丢弃，并把损失报出来。**

框架层统一（跑 N 遍、取一致、报损失），「一致」和「损失」的定义由观测层提供。

**丢弃率必须报出来**：丢太多说明这个 Subject 根本不确定，该**拒绝这道题**，
而不是悄悄缩小语料。

**按位置遮蔽必须先比对行数** —— 否则删掉一行会把后面的内容顶到被遮蔽的位置藏起来。

---

## 6. 计分

Verifier 产出两个量：**correctness ∈ [0,1]**（功能正确性达成比例）和 **speedup**（成本比值）。

```
score = 0.5 + 0.5 × speedup      if correctness == 1.0
score = 0.5 × correctness        otherwise

作弊审计独立生效：命中直接判 0，功能与计时都不再跑。
```

**正确性是解锁条件而非加权项**：没打满就拿不到任何速度分（最高 `0.5 × correctness`）；
打满后先拿 0.5 底分，再按加速比叠加。

**不封顶、不做对数映射、不设阈值。** 这是与 FrontierSWE-Performance 对齐的口径，
跨 benchmark 可比性高于任何曲线设计上的偏好。

**题面不写加速比阈值，只说「更快」。**

> ⚠️ **已写的 `frf/core/scoring.py` 与本节冲突**，实现时必须改。
> 它现在是「对数映射到 `[0.15, 1.0]`、2× 满分」的旧曲线，那是本节明确否决的形态。
> 这是本文件定稿前写的代码，**以本文件为准**。

### 三条计分执行细则

1. **噪声内按 1.0 计。** 落在现场标定的噪声地板之内的差异不是加速 ——
   这是测量事实，不是计分政策。
2. **多个隐藏测试取几何平均。** 比值的平均必须用几何平均，算术平均会被大数拖偏。
3. **每个 case 的比对结果都要记录。** correctness 需要它；而且「差 3 个」和「差一半」
   是完全不同的失败，只有逐 case 记录才能区分（实测：某提交 1564/1567，另一个不到 53%，
   两者的下一步完全不同）。

### 成本怎么测

- **配对交替**：参考与候选在同一批输入上成对运行，逐对轮换先后顺序。
  CPU 降频、邻近进程争抢等扰动因此同等地作用于两侧。
- **计时前预热**：正式测量前跑若干次不计时的，让缓存与编译达到稳定状态。
- **短函数重复调用**：单次太短测不准的，一次测量内重复调用到超过计时器精度下限。
- **噪声地板现场标定**：用「参考 vs 参考」测出**本次运行**的离散度。
  **不预设任何常数** —— 噪声是评测机器当时的属性，写死一个数就是在描述另一台机器。
- **按最差输入集计分，不合并**。实测案例：某提交渲染 2.97×、解析 1.47×，
  平均 2.2× 看起来通过，而那个数字**不对应任何真实调用方式**。

---

## 7. Adequacy：这批探测够不够

### 机制：覆盖率 → 暗区 → 补探测，循环

```
1. 参考带着覆盖率跑一遍现有语料   → 哪些行执行了(LIVE)、哪些没有(DARK)
2. DARK 行按所属函数聚合，挑基本没碰到的
3. 问模型：这些暗函数里，哪些是契约承诺要判的？（而不是时区、无关选项这类边角）
   对承诺要判的，让它提出具体的新输入
4. 新输入并进语料，重新冻结，回到 1
```

**为什么第 3 步需要模型**：暗区不等于该补。没被执行的代码可能是它承诺的功能（该补），
也可能是无关边角（补了只是撑大语料）。这个判断要读懂契约 ——
但**它只提议输入，不裁定对错**，新输入的正确答案照样由参考跑出来。

### 这份行为清单**不进题面**

清单只用于内部审计。**不发给解题者，这是难度的一部分** ——
「这程序到底承诺了什么」本身就是被考察的能力。参考实现就在工作区里，读它是解题者的活。

### 两个可测的数，缺一不可

| 数 | 问什么 | 单独看为什么会骗人 |
|---|---|---|
| **REACH** 行覆盖率 | 语料执行到了 Subject 的多少行 | 高覆盖率 ≠ 能分辨对错：执行到了但观测不到，照样是暗的 |
| **FLOOR** 白送率 | 什么都不做的提交已经能拿多少分 | 低白送率 ≠ 覆盖广：可能只是难蒙，但只考了一小块 |

**FLOOR 必须试多种平凡提交取最大值**，只试一种等于看运气 ——
实测同一道题上 `exit 0` 得 50%、`exit 1` 得 75%。

### 按尺度分叉的只有「测谁的覆盖率」

- **repo**：整个程序的行覆盖率。分母大得吓人，**只能当低门槛**，用来拒「几乎没碰到程序」的语料。
- **kernel / module**：被测符号自己的行。分母小，这个数才真正有意义。
- **package**：契约面上每个入口的行，外加**入口覆盖** —— 有没有哪个公开函数一次都没被调用。

---

## 8. Evidence：判分器自己要过的关

三条来自三个不同尺度的实战，各自堵的洞不一样，**取并集而不是取交集**。

| | 问什么 | 不加会怎样 | 适用 |
|---|---|---|---|
| **E1 天花板** | 参考实现必须拿满分 | 永远给 0 分的判分器能通过所有「坏提交得零分」的检查 | 全部 |
| **E2 地板** | 什么都不做必须得零分 | 判分器可能根本没在比 | 全部 |
| **E3 每条通道都咬人** | 每个计分通道单独扰动一次，都要被抓到 | 号称计分的通道其实是常数 | 全部 |
| **E4 不发可运行的参考** | 解题者能读到的目录里不能有能跑的参考 | `exec /reference/x` 就满分 | 全部 |
| **E5 计分点关于被测物** | 计分点不能落在从不调用 Subject 的步骤上 | 判的是包装的 shell，不是程序 | **仅接缝 A** |
| **E6 反作弊** | 候选不能把活转包给原实现 | 「重实现」变成「转发」 | 全部，**两种做法** |
| **E7 出厂包自证** | 用包里的参考驱动包里的判分器，必须满分 | **实测 14/78 个已出厂包栽在这条** | 全部 |
| **E8 种子无关** | 换哈希种子重跑，Expectation 必须一致 | 冻进去的其实是这次运行的字典序 | 全部 |

**E1 和 E2 必须成对** —— 只有 E2 会被「全判零分」的坏判分器通过，只有 E1 会被「全判满分」的通过。

**E3 最容易做成橡皮章**：变异体拿满分是**二义**的 —— 要么判分器瞎了（致命），
要么变异根本没碰到计分项（无意义）。只看分数分不出这两者的证明**本身就是橡皮章**。
所以 E3 不能从分数反推：必须把参考和变异体的观测直接 diff，**只有可证发散的才算证据**；
没发散的报「惰性」，全惰性则判「不确定」而不是通过。

**E5 只有接缝 A 需要，这不是偷懒。** 进程级的一个步骤可以是纯 shell、从不调用被测程序。
而接缝 B 的每一个观测**按构造**都来自 `entry(args)` 的返回值 —— 不调用就没有观测。

> **一个尺度不需要某条 Evidence，必须说得出「为什么它在结构上不可能发生」，
> 说不出就是漏了而不是不适用。**

### E4 有个容易漏的半边：判分器自己的目录

E4 通常被实现成「检查发给解题者的那棵参考树」。**这只是一半。**
判分器自己的目录里装着可运行的参考、Expectation 和全部探测输入 ——
如果判分器跑在**解题者的同一个环境里**，那个目录就是可读的。

抽查现有产出：仓库尺度 7/7 是 `separate`；**包尺度 91/91 是 `shared`**，
而它的 `cases.jsonl` 每行都是 `{"args": [...], "result": <正确答案>}` —— **明文答案**。
那种模式下，一份读文件并回放的提交可以拿满分。

> **本链路规定：`separate` 是默认且唯一，Expectation 只存哈希不存明文。
> E4 必须同时检查两个目录。**

### E6 反作弊：两条独立措施，都不用模型判断

**① 源码检查（功能测试之前）**
每道题带一份清单：允许哪些 import、禁止哪些调用。
逐一检查提交里出现的 import 和函数调用；命中禁止项**直接判 0，功能与计时都不再跑**。

**② 执行隔离（计时期间）**
参考与候选在**不同受限账户**下运行，各自限制可创建的进程数，
防止把计算转包给另一个进程逃避计时。**一侧计时时挂起另一侧的进程**，
使任何一方都无法通过占用处理器资源拉长对方的运行时间。

**两条都不涉及模型判断**：规则由题面固定，同一份提交每次判定相同。

**接缝 A 另有一条更强的**：判分器只看四通道，无法得知二进制是什么语言编的。
唯一可执行的手段是**把源语言工具链从镜像里按路径删除**。
注意光从 apt 列表减包无效 —— 基础镜像可能已经把工具链装在 `/usr/local` 下。
接缝 A 是「做不到」，接缝 B 是「做了会被发现」，强度不同，设计上接受这个差别。

---

## 9. 题面

**规则透明，答案不透明。**

| 必须说清 | 必须不说 |
|---|---|
| 这程序是什么、干什么用的 | 具体行为清单（§7：那是解题者该读出来的） |
| 怎么调用、怎么提交（`/app/run.sh <args...>`） | 任何期望输出 |
| 判几个场景、看哪些通道、共多少个观测点 | 哪些场景、哪些命令 |
| 正确性是解锁条件，速度分要正确性满分才有 | 加速比阈值（只说「更快」） |
| 约束：不许调用参考、断网、镜像里没有源语言工具链 | |

**题面里的每个数字都从冻结的 Expectation 里读出来**，不是模板里的常数 ——
题面因此**不可能**声称一个 Expectation 做不到的事（E5 守这条）。

**两处现状要改**：
1. 「程序是什么」现在只有约 160 字符，太干瘪，说不清这程序干什么用的。要写够。
2. 「Anything less scores zero」的措辞与新计分公式冲突 —— correctness 是有分的
   （`0.5 × correctness`）。不改会让解题者在差 3/1567 时直接放弃，
   而那种情况是**值得继续修**的。

---

## 10. SOURCE：只用可枚举索引

> **候选清单必须来自可枚举的索引。模型可以排序和筛选候选，不能产生候选。**

理由直接：问模型要包名，**答不出「还剩多少素材」**，而且它不会告诉你重复了或漏了。
索引可以翻页、可以记录翻到哪、可以算剩余量 —— 而那正是 scalability 的定义。

| 尺度 | 索引 |
|---|---|
| **repo** | GitHub 检索（语言 × 星数分段翻页） |
| **package** | 各语言 registry（PyPI / npm / crates.io / pkg.go.dev / Maven Central / RubyGems）+ **反向依赖图** |
| **module** | **两条都要**：① 扫源码（产能大、实现简单）② 挖 PR（「这个 commit 自称性能优化」，素材质量的先验） |
| **kernel** | 同 module，按数值特征筛（数组循环、嵌套 for、dtype） |

**反向依赖图**比下载量更贴合需求：被最多包依赖 = 契约面重要且稳定。

**机器筛（全部可判定，不问模型）**：有明确公开契约面 / 有自己的测试 / 纯计算少 I/O /
依赖闭包小（断网容器装得起来）/ 有 pin 得住的 release tag。

module 的两条索引归一化成同一个 `Candidate`，进同一个队列。

---

## 11. 任意语言：判分侧真无关，素材侧一次性投入

**实证**：`git → zig` 这道题出厂了，而 **zig 在框架代码里一次都没出现过** ——
它是通过 recipe 里的 `setup` 步骤（curl 一个 tarball）进来的。
已出厂的目标语言有 java / c / go / typescript / rust / cpp / zig 七种，
框架**没有为它们中的任何一个写过分支**。

| 组件 | 性质 | 缺失时 |
|---|---|---|
| **判分**（四通道 / wire 协议） | **真·语言无关** | —— |
| serve shim 模板（每语言约 30 行） | **数据** | 该语言不能做接缝 B |
| 工具链表（装什么 / 删哪些路径） | **数据** | 跨语言题削弱反作弊；recipe 可自行 curl |
| **coverage 后端** | **真代码**，每语言一个 | **题照出，少一个数并明说** |
| **registry client**（每个约 50 行） | **真代码** | 该生态暂无素材来源 |

**首批一次性铺开（已完成，每一项都在本机真跑过）**：

| 组件 | 数量 | 实现方式 | 实测 |
|---|---|---|---|
| serve shim | 9 个键 / 8 种语言 | python / js（TS 共用）/ ruby / go / rust / c（C++ 共用）/ java | 7 个模板全部**编译并跑通**完整探针 |
| coverage 后端 | 9 个键 / 8 种语言 | settrace · V8 · `go build -cover` · gcov · llvm-cov · JVMTI 自建 agent · Ruby `Coverage` | 全部**真测到未覆盖分支**，且语料变宽时数字上升 |
| registry client | 7 个 | PyPI / npm / crates.io / pkg.go.dev / Maven / RubyGems **+ GitHub** | 7 个全部**实网跑通**，翻页不重不漏 |

三张表都是**数据**，加第 9 种语言只加一行加一个文件，`core/` 一行不动 ——
这一条现在由 `test_any_language.py` 机械检查（`core/` 与 `observe/` 里
除 shim 表和 coverage 表外不允许出现任何语言名）。

两个值得记下的实现选择：
- **Java 没有标准库 coverage，JaCoCo 是三方 jar**，而题目要离线构建。
  所以自己写了个 80 行 JVMTI agent（`coverage/agents/jvmti_lines.c`），
  用 JVM 自己的行号表当分母 —— 比启发式猜哪行是代码更准。
- **Go 用 `go build -cover` 而不是 `go test -coverprofile`**：
  被测对象是通过 wire 服务的程序，没有测试二进制；
  用后者等于在量「我们自己生成的测试覆盖了多少」。

**这是一次性投入，不是持续成本** —— 加第 9 种语言时前 8 种一行不改。

**覆盖率不是闸门**，这是设计决定：没人给这门语言写后端，题照样出厂，
只是**少一个质量数并且明说少了**。假装每种语言都能读到内部执行行是不诚实的。

---

## 12. 硬经验：写代码时的约束

这些不是检查项，是**一旦写错就没有闸门能补救**的东西。每条都已经发生过。

1. **不可复现的行按位置遮蔽，不能整行丢弃。** 丢整行等于允许解题者删掉那行照样满分。
   遮蔽必须**先比对行数**。

2. **同语言题必须有真实计时。** 参考实现就在解题者手里，没有速度维度的话 `cp` 一下就是满分。
   注意：**不预判 headroom** —— 「这题有没有优化空间」交给解题者去发现。
   我们只保证时钟读得出来：重复调用撑到超过计时器精度；撑不到就是**测不了**，
   不是**没空间**。前者是关于我们秒表的诚实声明，后者是关于程序的猜测。

3. **计时用的输入必须从计分集里扣除。** 否则可在正确性阶段缓存结果、计时阶段回放，
   拿满分加任意加速比。

4. **凭据只有一个来源**：环境变量优先，其次 `.env`（gitignore），由唯一一个函数读取。
   凭据进容器走**环境变量**，不推文件 —— 推文件会把密钥写到沙箱磁盘上，还可能随产物回流。

5. **「零分」和「跑不起来」必须能分开。** 一个失败原因如果把「这素材不适合」和「我们的工具坏了」
   归成一类，修复循环就会把工具的 bug 当成素材问题，一直修错地方。

6. **出厂后必须驱动出厂包自己再验一遍**（E7）。所有其它闸门量的都是工作树，
   而实测 14/78 个已出厂包的参考过不了自己的 Expectation —— 它们全部「当时认证通过」。

---

## 13. 目录结构

```
frontier-refactor-factory/
  frf/
    core/                 完全不知道观测长什么样 —— 只经手 Observation 这个抽象
      pipeline.py           八个环节的驱动 + 闸门 + 归因（素材的锅 / 我们的锅）
      scale.py              一个尺度要回答的四个问题
      evidence.py           E1–E8 证据组
      scoring.py            correctness 解锁 speedup，不封顶
      timing.py             配对交替、预热、现场标定噪声地板、最差输入集；成本可插拔
      adequacy.py           REACH + FLOOR，两个数缺一不可
      sourcing.py           可枚举索引 + 去重记忆 + 覆盖度account
      statement.py          题面：规则透明、答案不透明
      harbor.py             出厂格式（唯一一份）
      sandbox.py            容器 backend 抽象
      credentials.py        唯一读取凭据的地方

    observe/              知道观测长什么样 —— 两个接缝各自完整
      process/              接缝 A：四通道，按位置遮蔽（先比行数）
      call/                 接缝 B：JSON wire
        protocol.py           wire 格式
        observation.py        返回值树 + 整条丢弃 + 丢弃率
        runner.py             把 subject 当独立进程持有
        stages.py             六个共享环节（freeze/adequacy/battery/emit/replay）
        shims/serve.py        每语言约 30 行的模板（数据，不是框架代码）
      probes/schema.py      声明式输入 schema + 采样（含 dtype/shape，kernel 靠它）
      compare/values.py     精确 / 结构 / 数值包络

    scales/               每个尺度只写它独有的那点  ← 尚未实现
    factory.py            公开接口：Factory / Settings / Result

  tests/                  只跑真实进程；mock 出来的测试测的是错的东西
  DESIGN.md  README.md  pyproject.toml
```

**代码规范**：全新编写，**不出现任何旧工厂的名字或缩写**。
模块文档解释**为什么这样设计**，而不是复述代码；每道闸门的注释写明它拦的是哪一类错误。

---

## 14. 接口：一个尺度要写多少代码

```python
class Scale(Protocol):
    """一个尺度 = 四个回答。八个环节由框架驱动，尺度只回答这四问。"""

    def find(self, budget: int) -> Iterable[Candidate]:
        """去哪找素材。必须来自可枚举索引（§10）。"""

    def specify(self, candidate: Candidate) -> Spec:
        """这一个候选怎么建、怎么调用。声明部分必须能逐字段校验。"""

    def observe(self) -> Observer:
        """接缝 A 还是接缝 B。"""

    def probes(self, spec: Spec) -> ProbeSource:
        """schema 采样器，或容器里跑的生成器。"""
```

框架负责的部分**对四个尺度逐字相同**：建环境、跑 5 遍冻结、补足充分性、搜索区分输入、
扣除计时探测、跑证据组、打 Harbor 包、驱动出厂包复验。

```python
class Observer(Protocol):
    def run(self, subject: Subject, probe: Probe) -> Observation: ...
    def compare(self, expected: Observation, actual: Observation) -> Verdict: ...
```

**判分器只依赖 `Observer` 这两个方法**，所以「加一个尺度」和「加一种观测」是两件独立的事。

### 落地判据

> **实现一个新尺度若需要改 `core/` 里的任何一行，说明这份抽象错了。**

这条比任何文档都硬。它当场就抓到过一个错误的划分：

我数了现有实现提到四通道的次数 —— 打分模块 **0 次**（真通用），
冻结模块 **28 次**（根本不通用）。也就是说「跑 N 遍冻结」这件事是通用的，
**但现有那份实现不是**：它的类型里写死了 `argv` / `stdout` / `tree`，
而接缝 B 的一次观测既没有 argv 也没有目录树。

**所以 `core/` 的边界按「依不依赖观测形状」划，不按「听起来通不通用」：**

```
core/     完全不知道观测长什么样：打分、证据、容器、出厂、检索
          ↑ 只处理 Observation 这个抽象，从不拆开看里面
observe/  知道观测长什么样：两个接缝各自的类型、冻结、比较
```

冻结算法写在 `observe/` 的两个实现里，而不是 `core/`。
**这不是重复代码 —— 是同一个思想在两种数据形状上的两次落地**，
硬合的代价是发明一个假的公共表示。

---

## 15. 尚未决定

- **各尺度的良率与成本，没有同口径数据。** repo 尺度实测：跨语言每仓库约 7%，
  同语言约 1.4%（走过 503 个仓库）。module / kernel / package 无数据 ——
  **打通后第一件事就是测**。
- **进程隔离对良率的影响**：Probe 必须 JSON 序列化，这会筛掉一部分候选。
  比例未知，必须在 §16 的里程碑中作为一个数报出来。
- **GPU 点亮时机**：接口按 Harbor 字段预留，何时接真卡另议。

**明确不做的**：难度刻度。只保留「能不能出厂」这个二元判据。

---

## 16. 第一个里程碑

「module 打通了」不能是一句感觉。定成一条可以被推翻的断言：

> **随便挑 20 个候选跑完整条链路，出厂的每一道都满足下面全部条件；
> 没出厂的每一道，失败原因都指向素材而不是工厂。**

| 条件 | 怎么验 |
|---|---|
| 参考在**出厂包里**拿满分 | 用包里的参考驱动包里的 Verifier（E7） |
| 平凡提交拿零分 | 至少三种平凡提交取最高分（E2 + FLOOR） |
| 每条计分通道都咬人 | 逐通道扰动，**观测必须可证发散**（E3） |
| 判分器目录读不到 | `separate` + Expectation 只存哈希（E4） |
| 反作弊两条都在 | import 清单 + 受限账户隔离（E6） |
| 覆盖到被测符号的大部分 | REACH 有下限，暗区已过一轮补探测（§7） |
| 计时读得出来 | 重复调用后超过计时器精度；读不出的**明说测不了** |

**同样重要的是第二半句**：一次失败必须能归到「这个候选不适合」还是「工厂坏了」。
这两类的比例本身就是产出物 —— 如果失败里大半是工厂的锅，那不叫良率低，叫没打通。

跑完这 20 个，同时得到四个之前拿不到的数：
**module 的真实良率**、**每题的机时与成本**、**失败原因的分布**、
**因 Probe 无法序列化而被拒的比例**（§15 第二条）。

先做 20 个而不是 5 个：良率如果在 5%~10% 这个量级，5 个候选有很大概率一道都不出 ——
那种结果既不能证明打通了，也不能证明没打通。

### 16.1 跑完了：实测结果

2026-08，20 个候选，全部来自 PyPI 真实 sourcing（`python-functions` 索引，
`subset="algorithm"`），冻结在 E2B 里跑。

| 数 | 实测 |
|---|---|
| **真实良率** | **11/20 = 55%** |
| **每题机时** | 12s（全批 137s，含下载、解包、扫描、5 次冻结、证据组、出厂） |
| **归因** | 素材 9，**工厂 0** — `trustworthy: True` |
| **Probe 无法序列化被拒** | 0 —— 但见下面那条，它被更早的闸门挡掉了 |

失败原因分布：

| 原因 | 数量 | 是什么 |
|---|---|---|
| `evidence/channels-bite` | 6 | subject 扰动不动（`min=max=array[0]` 改成 `array[-1]` 结果一样） |
| `freeze/will-not-repeat-itself` | 3 | 单独拎出来跑不起来（相对 import） |

**这一节的第二半句是靠改代码才成立的，不是一开始就成立。** 第一次跑出来
「工厂的锅 2/20」，恰好卡在 `trustworthy` 的 10% 线上。查下去两个都不是工厂的问题：
一个 subject 扰动不动，是素材的事实，不是我们没配好。归因表加了
`INCONCLUSIVE_IS_MATERIAL`，因为「查不出结论」默认是我们的锅，但这一条不是。

**顺带测出来一件事：诊断信息错了两天。** subject 起不来时报的是 stderr 的**开头**，
于是每一个失败候选的死因都显示成镜像打的 `dpkg: warning` 横幅。改成固定取结尾也不对，
会把 traceback 从中间切断，停在 `File "importlib/__init__.py", line 90`，永远看不到异常。
traceback 的异常在**最后一行**，所以取最后一行 + 它上面那个 frame。

改完之后真正的死因立刻现形，而且和之前显示的完全无关：10 个失败里 8 个是
`ModuleNotFoundError`（networkx / psutil / torch）。这是**扫描期就能判**的
—— 读文件自己的 import 语句即可，不用下载、解包、build、跑 5 遍冻结才发现。
挪进 `functions.py` 之后依赖类失败从 8 降到 3，省下的预算挖到了更深的素材，
良率从 50% 升到 55%。

**还没测到的**：§15 第二条那个「Probe 无法序列化」的比例。这批是 0，但不能说明问题——
schema 推断只接受它能画的类型，画不出来的函数在扫描期就没成为候选。要测这个数，
得放宽 `functions.py` 的类型表，让更奇怪的参数进来。
