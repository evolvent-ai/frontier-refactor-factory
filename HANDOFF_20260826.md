# Frontier Refactor Factory Handoff

## 目标

构建 open-world、多语言的四-scale 题目生产链路：GitHub 不限制语言；Repo 支持任意能在 E2B 中构建、启动并接受 deterministic workload 的 native 语言；Module/Kernel/Package 通过 adapter capability 逐步认证。公开 task/verifier/TOML/JSONL 协议不能改变，未知语言不能静默降级为 Python。

## 关键架构边界

- Repo 使用 process seam：exit code、stdout、stderr、filesystem diff。因此 Repo 可以 open-world，不限 Tier-1 语言。
- Module/Kernel/Package 使用独立进程 JSON call seam。函数加载、symbol dispatch、参数 schema、value/error codec、编译方式无法对未知语言安全猜测，必须通过注册 adapter。
- Capability 层级必须保留：`discovered -> repo-capable -> call-capable -> certified`。
- `frf/core/pipeline.py` 必须保持语言/scale 无关，不添加 `if language == ...` 分支。
- Harbor review/repair 是可选发布质量 gate，默认 `harbor_check=false`，不是默认生产 pipeline 阶段。

## 当前已完成并推送

- capability registry、open-world GitHub sourcing、language matrix、adapter/scale wiring。
- E2B remote backend、DinD 模板和 140 个 source/target Dockerfile 组合：`140/140 passed`。
- checkpoint、ledger、active sandbox 限制；`--workers 32 --active 8` 已验证 `peak_active=8` 且无 failure。
- Module/Kernel/Package/Repo 共用语言无关 pipeline；freeze(5)、adequacy、evidence、E7 replay gates 已接通。
- Harbor 可选 review wrapper：dotenv 凭据桥接、Chat Completions-compatible `mini-swe-agent`、模型名 `openai/<model>` 规范化、job/template alias 清洗、外层并发限制、唯一 job name、有限重试、单任务异常隔离。
- Go Harbor review 临时 wrapper：toolchain fallback、module download、offline 时复用已 replay 的 native binary；不修改 emitted task。
- Harbor schema-only audit：已生成任务可逐任务 schema 验证。
- language matrix 支持 `--scale all`，并记录 source eligibility、adapter status、阶段状态、yield、seconds、concurrency；空 source 显式标记 `source_eligibility=empty`。
- 默认生产批次证据：Module Python `4/4 emitted`、Package Python `4/4 emitted`、Repo Go `3/15 emitted`（yield 20%，trustworthy=true），另有 Repo Python/Go 真实任务和 JavaScript Package task。
- 测试基线曾达到 `199 passed, 15 skipped`；最近修改后至少相关 Harbor/matrix 测试通过，重新开始工作时必须再跑全量。

## 当前真实缺口

1. Tier-1 语言族（Python、JavaScript、TypeScript、Go、Rust、Java、Ruby、C/C++）并非全部在每个 scale certified。Python 证据最多；JavaScript Package 已有真实 task；其他组合仍需独立 batch。
2. JS/TS Module/Kernel function miner 尚未完成：当前 function miner 主要扫描 Python AST，不能因为已有 JS shim 就宣称 call-capable/certified。
3. Package 多候选稳定 yield 需要按语言积累，不可用单个成功样本替代。
4. Repo 虽 open-world，但各语言仍需真实 source→static gate→E2B smoke→build→workload→freeze→adequacy→evidence→replay 证据。
5. Harbor agent review 不是默认生产步骤；只有显式开启时才运行。第三方 Harbor `harbor run`/agent review 可能因具体任务环境、模型输出或外部 template 失败，这些要按 task 记录，不能放宽质量 gate。
6. 最近的 source eligibility 提交位于本地历史 `cecc9b8`，当前 `main...origin/main` 显示同步；确认状态后再继续提交。

## 新 session 推荐顺序

### A. 先做基线

```bash
cd /data/evolvent/shijian-workdir/frontier-refactor-factory
.venv/bin/pytest -q
.venv/bin/python scripts/validate_scalability.py --workers 32 --active 8
.venv/bin/python scripts/harbor_run_validate.py --schema-only tasks/e2b_test
git status --short --branch
```

### B. 生产 batch（默认不启用 Harbor）

先按语言/scale 分开跑，保存 stdout JSON 和 ledger；不要把不同语言混在一个 yield 分母里：

```bash
FRF_LLM_TIMEOUT=120 .venv/bin/frf run --scale repo --form inplace --source go --budget 3
FRF_LLM_TIMEOUT=120 .venv/bin/frf run --scale repo --form inplace --source rust --budget 3
FRF_LLM_TIMEOUT=120 .venv/bin/frf run --scale package --form inplace --source python --budget 3
FRF_LLM_TIMEOUT=120 .venv/bin/frf run --scale package --form inplace --source javascript --budget 3
```

### C. 可选 Harbor review

仅对已通过默认 pipeline 的 emitted task 显式运行：

```bash
.venv/bin/python scripts/harbor_check_e2b.py tasks/<batch> \
  --model gpt-5.6-terra --agent mini-swe-agent --concurrent 4 --attempts 2
```

这里的 `--concurrent` 是外层 task review 上限；不要再次用无界后台循环。失败 task 不计入最终 emitted。

### D. 认证规则

- 只有真实 E2B、verifier、replay、Harbor（若该组合声明开启）和多个 candidate/batch 稳定 yield 证据齐全，才可把组合升级为 `certified`。
- `toolchain build passed` 只说明可构建，不等于 task certified。
- 未知语言保留为 `discovered`；Repo 可继续尝试，call-seam scale 必须显式拒绝并保留原因。
- 所有 commit message 使用英文；不修改 Harbor 第三方包源码，不修改公开 task/verifier 协议。

## 重要历史故障与处理

- Harbor 默认 Codex agent 使用 Responses/WebSocket，与 FRF Chat Completions gateway 不匹配；默认已切换 `mini-swe-agent`。
- Harbor/FRF 都会从不同位置读凭据；wrapper 使用 `frf.core.credentials.get()` 桥接 `.env` 到 `OPENAI_*`、`E2B_*`。
- Harbor template alias 不能含 `@` 等字符；wrapper 会清洗临时 wrapper 目录和 job name，原 task 名称不改。
- Harbor 外层 parent audit 需要 semaphore；单 task exception 不能取消整批。
- Go fresh E2B build 常无 module cache；review wrapper 可使用已通过 FRF E7 的 native binary，仅用于 review wrapper。
- 不要把 Harbor review 当作默认生产 pipeline；该功能默认关闭是有意设计。

## 完成定义

最终不能只看 tests 绿。必须有按 language × scale 的可审计矩阵、多个 candidate/batch 稳定 yield、真实 E2B/replay/quality evidence、干净 worktree 和已 push 的 commit，并明确区分 `certified`、`call-capable`、`repo-capable`、`discovered`。
